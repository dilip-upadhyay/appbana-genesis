{
  "name": "@appbana/ai-provenance-store",
  "version": "0.1.0",
  "private": true,
  "description": "Append-only store for AIProvenanceRecord. Provides a portable interface + two reference backends (in-memory and JSONL-file) + a kernel-side `assertProvenance` guard that refuses AI outputs without a valid provenance record. Postgres DDL shipped as ops artifact; driver is v0.2.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "sql", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types \"__tests__/*.test.ts\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@appbana/adapter-ai-contract": "file:../adapter-ai-contract"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "rimraf": "^6.0.1",
    "typescript": "5.6.3"
  }
}
